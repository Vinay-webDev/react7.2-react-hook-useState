/* react hook = a special function that allows functional
                components to use react features without 
                writing class components 
                (useState, useEffect, useReducer, useContext,
                useCallback and more.....)
  useState() = a react hook that allows creation of stateful
               variable and a setter function to update it's 
               value in the virtual DOM.  */
import Component from './Component.jsx'

function App() {
  

  return (
    <>
      <Component/>
    </>
  )
}

export default App
